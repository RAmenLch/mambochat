# MCP_SERVER/bing/pyproject.toml

[project]
name = "knowledge_base_mcp"
version = "1.0.0"
description = "内部资源库调用MCP服务"
requires-python = ">=3.11"
dependencies = [
    "mcp",              # 对应 from mcp.server.fastmcp import FastMCP
    "httpx",            # 对应 import httpx
]

[tool.uv]
# 可选配置
